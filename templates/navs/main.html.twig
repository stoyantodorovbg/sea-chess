<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            {% if app.request.get('_route') is not same as('app_game') %}
                <li class="m-2"><a href="{{ path('app_game') }}">Home</a></li>
            {% endif %}
            {% if app.user %}
                <li class="m-2"><a href="{{ path('app_logout') }}">Logout</a></li>
            {% else %}
                {% if app.request.get('_route') is not same as('app_login') %}
                    <li class="m-2"><a href="{{ path('app_login') }}">Login</a></li>
                {% endif %}
                {% if app.request.get('_route') is not same as('app_register') %}
                    <li class="m-2"><a href="{{ path('app_register') }}">Register</a></li>
                {% endif %}
            {% endif %}
        </ul>
    </div>
</nav>